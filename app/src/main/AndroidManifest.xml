<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cce.nkfust.tw.bentoofking">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppStartTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".BecomeStoreActivity" />
        <activity android:name=".BecomeHaveStoreActivity" />
        <activity android:name=".BecomeNotHaveStoreActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".CheckStoreInfo" />
        <activity android:name=".StoreLoginActivity" />
        <activity
            android:name=".AboutActivity"
            android:theme="@style/AppStartTheme" />
        <activity
            android:name=".EditDataBaseCommentDialog"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            android:theme="@style/Theme.ActivityDialogStyle" />
        <activity android:name=".EditMenuActivity" />
        <activity android:name=".EditPhotoActivity" />
        <activity android:name=".EditStoreActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".CommentAddStore" />
        <activity android:name=".EditMember" />
        <activity android:name=".ReviewStoreActivity" />
        <activity android:name=".EditExistedMenuActivity" />
        <activity android:name=".EditExistedPhotoActivity" />
        <activity android:name=".PreviewStoreInfoActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".HomeMapsActivity"
            android:label="@string/title_activity_home_maps" />
        <activity android:name=".MyFavoriteStore" />
        <activity android:name=".StoreErrorActivity" />
        <activity android:name=".CheckStoreErrorActivity" />
        <activity android:name=".CheckMemberActivity" />
        <activity android:name=".AppealCommentActivity" />
        <activity android:name=".BanedMemberActivity" />
        <activity android:name=".StoreAppealActivity" />
        <activity android:name=".CheckStoreAppealActivity" />
        <activity android:name=".MemberAppealActivity" />
        <activity android:name=".CheckMemberAppealActivity" />
        <activity android:name=".CheckAppealCommentActivity" />
        <activity android:name=".PushManageActivity" />
        <activity
            android:name=".CheckOpponentList"
            android:theme="@style/AppOpponentTheme" />
        <activity
            android:name=".CheckOpponent"
            android:theme="@style/AppOpponentTheme" />
        <activity android:name=".ReviewPushActivity" />

        <service android:name=".MyInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".OrderListActivity" />
        <activity android:name=".CheckOrderActivity" />
        <activity android:name=".OrderFinalActivity" />
        <activity android:name=".StoreAppealListActivity" />
        <activity android:name=".MemberAppealListActivity"></activity>
    </application>

</manifest>